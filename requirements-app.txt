# This is used to create Postgres stored functions via the migrations process.
alembic_utils >= 0.8.1

Authlib >= 1.2.0
boto3 >= 1.26.158
botocore >= 1.29.158
cachelib >= 0.10.2

# 5 May 2022: Ignoring security vulnerabilities 42498 and 43738 in Makefile.
# One is fixed in version 5.2.0; the other, 5.2.2.  See the note below for "kombu".
celery[sqs] >= 4.4.7

click-datetime >= 0.2

# Datadog Tracer and Profiler
ddtrace >= 1.15.0

docopt >= 0.6.2
eventlet ~= 0.33.3

# This is for hardware based authentication schemes.  (a.k.a. 2FA with security keys)
# https://developers.yubico.com/WebAuthn/
fido2 >= 1.1.1

Flask >= 2.3.2
Flask-Bcrypt >= 1.0.1
Flask-Cors >= 3.0.10
Flask-JWT-Extended >= 4.5.2

# 19 April 2023: v0.15.0 depends on marshmallow >= 3.0.0.
flask-marshmallow ~= 0.14.0

Flask-Migrate >= 4.0.4
Flask-SQLAlchemy >= 3.0.5
git+https://github.com/department-of-veterans-affairs/notification-utils.git

# The production image defined by Dockerfile uses Gunicorn.  The development image defined by Dockerfile.local
# runs the application with the Flask development server by default.
#
# This is Gunicorn v20.1.0 with additional fixes for import errors that occur with eventlet > 0.30.2.
gunicorn[eventlet] @ git+https://github.com/benoitc/gunicorn.git@6a8ebb4844b2f28596ffe7421eb9f7d08c8dc4d8

iso8601 >= 2.0.0
itsdangerous >= 1.1.0
jsonschema >= 4.17.3

# 5 May 2022: Celery depends on this package, and the Docker image build failed with the newest version.
# Ignoring security vulnerability 42497 in Makefile.  The vulnerability is fixed in kombu >= 5.2.1.
# Kombu is a top-level package too.
kombu < 5.0

# 31 March 2023: marshmallow 2.x has not been supported since 18 August 2020.
marshmallow ~= 2.20.5
marshmallow-sqlalchemy ~= 0.22.3
nanoid >= 2.0.0
notifications-python-client >= 8.0.1
psycopg2-binary >= 2.9.6
pwnedpasswords >= 2.0.0
PyJWT >= 2.7.0

# 19 April 2023: This package is deprecated.  See #1221.
PyPDF2 ~= 3.0.1

python-dotenv >= 1.0.0
python-magic >= 0.4.27

# jsonschema requires this package to validate the date-time format but doesn't include it as a dependency.
# Validation fails silently without it.
# https://python-jsonschema.readthedocs.io/en/latest/validate/?highlight=date-time#validating-formats
rfc3339-validator >= 0.1.4

sentry-sdk[flask] >= 1.25.1

# 10 December 2022: Ignoring security vulnerability 51668 in Makefile.  It is fixed in  >= 2.0.0b1.
SQLAlchemy ~= 1.4.48

twilio >= 8.3.0
Unidecode >= 1.3.6
validatesns >= 0.1.1
